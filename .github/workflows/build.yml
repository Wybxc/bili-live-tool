name: Build & Release

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  FLOATING_TAG: nightly

jobs:
  build:
    name: ${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            runner: ubuntu-latest
            bundle-format: deb
            artifact-suffix: x86_64-linux

          - target: aarch64-apple-darwin
            runner: macos-latest
            bundle-format: osx
            artifact-suffix: aarch64-macos

          - target: x86_64-pc-windows-msvc
            runner: windows-latest
            bundle-format: ""
            artifact-suffix: x86_64-windows

    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            fakeroot \
            dpkg-dev \
            libfontconfig-dev \
            libxkbcommon-dev \
            libxkbcommon-x11-dev \
            libxcb1-dev

      - name: Install cargo-bundle
        run: cargo install cargo-bundle

      - name: Build bundle (Linux / macOS)
        if: matrix.bundle-format != ''
        run: cargo bundle --release --target ${{ matrix.target }} --format ${{ matrix.bundle-format }}

      - name: Build binary (Windows)
        if: runner.os == 'Windows'
        run: cargo build --release --target ${{ matrix.target }}

      - name: Create DMG (macOS)
        if: runner.os == 'macOS'
        run: |
          APP="target/${{ matrix.target }}/release/bundle/osx/Bili Live Tool.app"
          if [ -d "$APP" ]; then
            codesign --force --deep --sign - "$APP"
            STAGING=$(mktemp -d)
            cp -R "$APP" "$STAGING/"
            ln -s /Applications "$STAGING/Applications"
            hdiutil create \
              -volname "Bili Live Tool" \
              -srcfolder "$STAGING" \
              -ov -format UDZO -imagekey zlib-level=9 \
              "bili-live-tool-${{ matrix.artifact-suffix }}.dmg"
            rm -rf "$STAGING"
          fi

      - name: Package binary (Windows)
        if: runner.os == 'Windows'
        run: |
          cd "target/${{ matrix.target }}/release"
          7z a "../../../bili-live-tool-${{ matrix.artifact-suffix }}.zip" bili-live-tool.exe

      - uses: actions/upload-artifact@v4
        with:
          name: bundle-${{ matrix.artifact-suffix }}
          path: |
            target/${{ matrix.target }}/release/bundle/
            bili-live-tool-${{ matrix.artifact-suffix }}.dmg
            bili-live-tool-${{ matrix.artifact-suffix }}.zip
          if-no-files-found: warn

  release:
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Collect release files
        run: |
          mkdir -p release-files
          find artifacts -type f \
            \( -name "*.deb" -o -name "*.dmg" -o -name "*.zip" \) \
            -exec cp {} release-files/ \;
          echo "=== Release files ==="
          ls -lh release-files/

      - name: Delete old nightly release
        run: |
          gh release delete "$FLOATING_TAG" --yes --repo "${{ github.repository }}" 2>/dev/null || true
          git push origin --delete refs/tags/"$FLOATING_TAG" 2>/dev/null || true
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Create nightly release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.FLOATING_TAG }}
          name: "Nightly Build"
          body: |
            Automatic build from [`${{ github.sha }}`](https://github.com/${{ github.repository }}/commit/${{ github.sha }})
          prerelease: true
          files: release-files/*
          fail_on_unmatched_files: false
